import Constants from 'expo-constants';

export const propertiesApi = `${Constants.manifest?.extra?.BASE_URL}/properties`;
export const usersApi = `${Constants.manifest?.extra?.BASE_URL}/users`;
export const signUpApi = `${Constants.manifest?.extra?.BASE_URL}/signUp`;
